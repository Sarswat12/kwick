# Frontend configuration example for local development
# Copy this to .env and fill values. Do NOT commit .env

VITE_API_BASE_URL=http://localhost:5000/api
VITE_ALLOW_DEV_AUTOLOGIN=false
# Payment provider keys (use sandbox keys)
VITE_RAZORPAY_KEY=

# DO NOT store production secrets in git. Use secret manager in production.
